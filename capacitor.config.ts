import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hospital.management',
  appName: '\x17\x17LivSeva',
  webDir: 'build'
};

export default config;
